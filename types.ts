let firstName: string = 'satuk',
    lastName: string = 'kolan',
    age: number = 33;

function tell( firstName: string, lastName: string, age: number ): void {
    console.log( 'my name is ' + firstName + ' ' + lastName + ' and i am ' + age + ' years old.' );
}

tell( firstName, lastName, age );
